# --- Security & Personal Data ---
# Do NOT remove these; they protect your keys and progress
config.py
brain.json
project_state.md
goal_prompt
.env

# --- Python Clutter ---
__pycache__/
*.py[cod]
*$py.class
venv/
env/

# --- Vendored / Derived Data ---
# Offline stroke-order database (~6.6 MB), rebuilt with fetch_stroke_data.py.
# vendor/hanzi-writer.min.js IS tracked -- it's 36 KB and pins the library
# version the app was tested against.
stroke_data.json

# --- Operating System Junk ---
# NOTE: gitignore has no trailing-comment syntax, so these previously read as
# literal patterns ending in "# macOS folder attributes" and matched nothing.
.DS_Store
Thumbs.db
*.swp
*~

# --- Web/PWA Clutter ---
node_modules/
.eslintcache
